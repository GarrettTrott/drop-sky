import React from 'react'
import PropTypes from 'prop-types'

const TempBar = ({ hourlyTemp }) => {
  return <div></div>
}

TempBar.propTypes = {
  hourlyTemp: PropTypes.array,
}

export default TempBar
